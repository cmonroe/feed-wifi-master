--- a/mt7915/main.c
+++ b/mt7915/main.c
@@ -252,6 +252,9 @@ static int mt7915_add_interface(struct i
 		vif->offload_flags = 0;
 	vif->offload_flags |= IEEE80211_OFFLOAD_ENCAP_4ADDR;
 
+	if (vif->type == NL80211_IFTYPE_STATION)
+		vif->offload_flags = 0;
+
 	mt7915_init_bitrate_mask(vif);
 	memset(&mvif->cap, -1, sizeof(mvif->cap));
 
