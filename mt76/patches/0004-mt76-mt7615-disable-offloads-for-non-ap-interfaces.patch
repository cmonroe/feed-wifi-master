--- a/mt7615/main.c
+++ b/mt7615/main.c
@@ -241,6 +241,9 @@ static int mt7615_add_interface(struct i
 	}
 
 	ret = mt7615_mcu_add_dev_info(phy, vif, true);
+
+	if (vif->type == NL80211_IFTYPE_STATION)
+		vif->offload_flags = 0;
 out:
 	mt7615_mutex_release(dev);
 
