--- a/mt7996/mac.c
+++ b/mt7996/mac.c
@@ -1081,7 +1081,7 @@ mt7996_txwi_free(struct mt7996_dev *dev,
 		goto out;
 
 	txwi = (__le32 *)mt76_get_txwi_ptr(mdev, t);
-	if (link_sta) {
+	if (link_sta && wcid) {
 		wcid_idx = wcid->idx;
 		if (likely(t->skb->protocol != cpu_to_be16(ETH_P_PAE))) {
 			struct mt7996_sta *msta;
