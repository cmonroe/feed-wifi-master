--- a/mt7915/mcu.c
+++ b/mt7915/mcu.c
@@ -3073,6 +3073,9 @@ int mt7915_mcu_apply_tx_dpd(struct mt791
 	u8 dpd_mask, cal_num = is_mt7915(&dev->mt76) ? 2 : 3;
 	int idx;
 
+	/* skip DPD pre-cal for now */
+	return 0;
+
 	switch (band) {
 	case NL80211_BAND_2GHZ:
 		dpd_mask = MT_EE_WIFI_CAL_DPD_2G;
