--- a/mt7915/main.c
+++ b/mt7915/main.c
@@ -350,6 +350,10 @@ int mt7915_set_channel(struct mt7915_phy
 	mt7915_mac_reset_counters(phy);
 	phy->noise = 0;
 
+	ret = mt7915_mcu_set_txpower_sku(phy);
+	if (ret)
+		return ret;
+
 out:
 	clear_bit(MT76_RESET, &phy->mt76->state);
 	mutex_unlock(&dev->mt76.mutex);
