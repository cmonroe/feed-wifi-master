--- a/mt7915/main.c
+++ b/mt7915/main.c
@@ -336,7 +336,7 @@ int mt7915_set_channel(struct mt7915_phy
 	if (dev->flash_mode) {
 		ret = mt7915_mcu_apply_tx_dpd(phy);
 		if (ret)
-			goto out;
+			dev_dbg(dev->mt76.dev, "no TX DPD data for desired channel");
 	}
 
 	ret = mt7915_mcu_set_chan_info(phy, MCU_EXT_CMD(CHANNEL_SWITCH));
