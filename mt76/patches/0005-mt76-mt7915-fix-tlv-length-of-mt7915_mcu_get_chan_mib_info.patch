--- a/mt7915/mcu.c
+++ b/mt7915/mcu.c
@@ -3044,7 +3044,7 @@ int mt7915_mcu_get_chan_mib_info(struct
 	}
 
 	ret = mt76_mcu_send_and_get_msg(&dev->mt76, MCU_EXT_CMD(GET_MIB_INFO),
-					req, sizeof(req), true, &skb);
+					req, len * sizeof(req[0]), true, &skb);
 	if (ret)
 		return ret;
 
