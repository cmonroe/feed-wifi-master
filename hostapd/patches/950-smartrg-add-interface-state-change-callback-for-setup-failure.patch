From: Chad Monroe <chad@monroe.io>
Date: Tue, 25 Nov 2025 10:22:00 -0800
Subject: [PATCH] hostapd: add interface state change callback for setup failure

Notify netifd when interface transitions to DISABLED from ACS, DFS, or
HT_SCAN states. Allows netifd to retry setup in case of a transient failure.

Suppress failure notification when the transition is caused by an
intentional AP-STA coordination stop (apsta_state down). Without this
guard, STA disconnect on a DFS channel triggers DFS_FAILED, causing
netifd to teardown and restart the radio in a loop.

Signed-off-by: Chad Monroe <chad@monroe.io>
---
 openwrt/files/hostapd.uc |   59 +++++++++++++++++++++++++++++++++++--
 src/ap/hostapd.c         |    6 +++
 src/ap/ucode.c           |   13 ++++++++
 src/ap/ucode.h           |   11 ++++++
 4 files changed, 85 insertions(+), 4 deletions(-)

--- a/src/ap/hostapd.c
+++ b/src/ap/hostapd.c
@@ -57,6 +57,7 @@
 #include "airtime_policy.h"
 #include "wpa_auth_kay.h"
 #include "hw_features.h"
+#include "ucode.h"
 
 
 static int hostapd_flush_old_stations(struct hostapd_data *hapd, u16 reason);
@@ -4353,10 +4354,13 @@ const char * hostapd_state_text(enum hos
 
 void hostapd_set_state(struct hostapd_iface *iface, enum hostapd_iface_state s)
 {
+	enum hostapd_iface_state old_state = iface->state;
+
 	wpa_printf(MSG_INFO, "%s: interface state %s->%s",
 		   iface->conf ? iface->conf->bss[0]->iface : "N/A",
-		   hostapd_state_text(iface->state), hostapd_state_text(s));
+		   hostapd_state_text(old_state), hostapd_state_text(s));
 	iface->state = s;
+	hostapd_ucode_iface_state_cb(iface, old_state, s);
 }
 
 
--- a/src/ap/ucode.c
+++ b/src/ap/ucode.c
@@ -1305,3 +1305,16 @@ void hostapd_ucode_apup_newpeer(struct h
 	ucv_put(val);
 }
 #endif // def CONFIG_APUP
+
+void hostapd_ucode_iface_state_cb(struct hostapd_iface *iface,
+				  enum hostapd_iface_state old_state,
+				  enum hostapd_iface_state new_state)
+{
+	if (wpa_ucode_call_prepare("iface_state"))
+		return;
+
+	uc_value_push(ucv_string_new(iface->phy));
+	uc_value_push(ucv_int64_new(old_state));
+	uc_value_push(ucv_int64_new(new_state));
+	ucv_put(wpa_ucode_call(3));
+}
--- a/src/ap/ucode.h
+++ b/src/ap/ucode.h
@@ -4,6 +4,8 @@
 #include "utils/ucode.h"
 
 struct hostapd_data;
+struct hostapd_iface;
+enum hostapd_iface_state;
 
 struct hostapd_ucode_bss {
 #ifdef UCODE_SUPPORT
@@ -45,6 +47,10 @@ struct wpabuf *hostapd_ucode_dpp_gas_req
 					 const u8 *query, size_t query_len);
 #endif /* CONFIG_DPP */
 
+void hostapd_ucode_iface_state_cb(struct hostapd_iface *iface,
+				  enum hostapd_iface_state old_state,
+				  enum hostapd_iface_state new_state);
+
 #else
 
 static inline int hostapd_ucode_init(struct hapd_interfaces *ifaces)
@@ -77,6 +83,11 @@ static inline void hostapd_ucode_wps_m7_
 					    int *skip_cred)
 {
 }
+static inline void hostapd_ucode_iface_state_cb(struct hostapd_iface *iface,
+						enum hostapd_iface_state old_state,
+						enum hostapd_iface_state new_state)
+{
+}
 
 #ifdef CONFIG_DPP
 static inline int hostapd_ucode_dpp_rx_action(struct hostapd_data *hapd,
--- a/openwrt/files/hostapd.uc
+++ b/openwrt/files/hostapd.uc
@@ -62,6 +62,12 @@ hostapd.data.bss_info_fields = {
 hostapd.data.mld = {};
 hostapd.data.dpp_hooks = {};
 
+const HAPD_IFACE_DISABLED = 1;
+const HAPD_IFACE_ACS = 3;
+const HAPD_IFACE_HT_SCAN = 4;
+const HAPD_IFACE_DFS = 5;
+const NOTIFY_CMD_SETUP_FAILED = 5;
+
 function iface_remove(cfg)
 {
 	if (!cfg || !cfg.bss || !cfg.bss[0] || !cfg.bss[0].ifname)
@@ -848,24 +854,30 @@ function iface_set_config(name, config)
 	let old_config = hostapd.data.config[name];
 
 	hostapd.data.config[name] = config;
+	hostapd.data.configuring = name;
 
 	let phy = config.phy;
 	let phydev = phy_open(phy, config.radio_idx);
 	if (!phydev) {
 		hostapd.printf(`Failed to open phy ${phy}`);
+		delete hostapd.data.configuring;
 		return false;
 	}
 
 	config.orig_bss = [ ...config.bss ];
 	iface_check_mld(phydev, name, config);
-	if (!length(config.bss))
-		return iface_config_remove(name, old_config);
+	if (!length(config.bss)) {
+		let ret = iface_config_remove(name, old_config);
+		delete hostapd.data.configuring;
+		return ret;
+	}
 
 	try {
 		let ret = iface_reload_config(name, phydev, config, old_config);
 		if (ret) {
 			iface_update_supplicant_macaddr(phydev, config);
 			hostapd.printf(`Reloaded settings for phy ${name}`);
+			delete hostapd.data.configuring;
 			return 0;
 		}
 	} catch (e) {
@@ -875,6 +887,7 @@ function iface_set_config(name, config)
 	hostapd.printf(`Restart interface for phy ${name}`);
 	let ret = iface_restart(phydev, config, old_config);
 
+	delete hostapd.data.configuring;
 	return ret;
 }
 
@@ -1268,7 +1281,9 @@ let main_obj = {
 				return 0;
 
 			if (!req.args.up) {
+				hostapd.data.apsta_stopping = phy;
 				iface.stop();
+				delete hostapd.data.apsta_stopping;
 				return 0;
 			}
 
@@ -1375,6 +1390,7 @@ let main_obj = {
 			radio: 0,
 			config: "",
 			prev_config: "",
+			device: "",
 		},
 		call: function(req) {
 			let phy = req.args.phy;
@@ -1382,18 +1398,23 @@ let main_obj = {
 			let name = phy_name(phy, radio);
 			let file = req.args.config;
 			let prev_file = req.args.prev_config;
+			let device = req.args.device;
 
 			if (!phy)
 				return libubus.STATUS_INVALID_ARGUMENT;
 
 			if (prev_file && !hostapd.data.config[name]) {
 				let config = iface_load_config(phy, radio, prev_file);
-				if (config)
+				if (config) {
 					config.radio.data = [];
+					config.device = device;
+				}
 				hostapd.data.config[name] = config;
 			}
 
 			let config = iface_load_config(phy, radio, file);
+			if (config && device)
+				config.device = device;
 
 			hostapd.printf(`Set new config for phy ${name}: ${file}`);
 			iface_set_config(name, config);
@@ -1612,4 +1633,38 @@ return {
 			return null;
 		return response;
 	},
+	iface_state: function(phy, old_state, new_state) {
+		if (new_state != HAPD_IFACE_DISABLED)
+			return;
+
+		if (old_state != HAPD_IFACE_ACS &&
+		    old_state != HAPD_IFACE_DFS &&
+		    old_state != HAPD_IFACE_HT_SCAN)
+			return;
+
+		if (hostapd.data.configuring == phy)
+			return;
+
+		if (hostapd.data.apsta_stopping == phy)
+			return;
+
+		let cfg = hostapd.data.config[phy];
+		if (!cfg || !cfg.device)
+			return;
+
+		let reason;
+		if (old_state == HAPD_IFACE_ACS)
+			reason = "ACS_FAILED";
+		else if (old_state == HAPD_IFACE_DFS)
+			reason = "DFS_FAILED";
+		else
+			reason = "HT_SCAN_FAILED";
+
+		hostapd.printf(`Interface ${phy} setup failed: ${reason}`);
+		ubus.call("network.wireless", "notify", {
+			"command": NOTIFY_CMD_SETUP_FAILED,
+			"device": cfg.device,
+			"data": { "reason": reason }
+		});
+	},
 };
